{
  "name": "jumbotail-assignment",
  "version": "1.0.0",
  "description": "Jumbotail B2B E-Commerce Shipping Charge Estimator — Full Stack",
  "scripts": {
    "start": "node backend/src/index.js",
    "dev:backend": "cd backend && npm run dev",
    "dev:frontend": "cd frontend && npm run dev",
    "render-build": "cd backend && npm install && cd ../frontend && npm install && npm run build",
    "db:init": "cd backend && node src/db/init.js"
  },
  "engines": {
    "node": ">=18.0.0"
  },
  "license": "MIT"
}
